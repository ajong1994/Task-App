<p id="notice"><%= notice %></p>
<p>
    <strong>Task Name:</strong>
    <%= @task.name%>
</p>
<p>
    <strong>Details</strong>
</p>
<p><%= @task.body%></p>
<p><span>Due Date:</span> <%= @task.task_date%></p>
<%= link_to 'Edit', edit_category_task_path(@task) %> |
<%= link_to 'Back', category_path(@category) %>
