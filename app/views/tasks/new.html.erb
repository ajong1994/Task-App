<div class="container">
    <h2>New Task</h2>

    <%= render 'form', task: @task, url: category_tasks_path(@category.id), method: :post%>

    <p><%= link_to 'Back', category_path(@category.id) %></p>
</div>
