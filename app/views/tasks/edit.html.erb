<h1>Editing Task</h1>

<%= render 'form', task: @task, url: category_task_path(@category.id, @task.id), method: :patch%>

<%= link_to 'Show', category_task_path(@category.id, @task.id) %> |
<%= link_to 'Back', category_path(@category.id) %>
