<h1>Editing Task</h1>

<%= render 'form', task: @task, url: edit_category_task_path(@category.id, @task.id)%>

<%= link_to 'Show', category_task_path(@category.id, @task.id) %> |
<%= link_to 'Back', category_tasks_path(@category.id) %>
